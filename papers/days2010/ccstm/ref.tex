The most fundamental data type in CCSTM is \xtypeA{Ref}, which is a
mutable cell holding a value of type \xtypeparam{A}.  The fundamental
operations on \xtype{Ref} are \xcode{get} and \xcode{set}, which perform
a transactional read and a transactional write, respectively.
